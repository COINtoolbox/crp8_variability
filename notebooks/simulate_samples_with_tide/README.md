## Usage

Configure simulation parameters in the JSON config file (config.json)
- distance_mpc or redshift: Used in flux to luminosity conversion, specify one in the config

Run the simulation:

    python sample_tdes.py

Output CSV files will be saved to the configured output directory.
